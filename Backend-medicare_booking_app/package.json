{
  "name": "backend-medicare_booking_app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker-compose up --build",
    "down": "docker-compose down",
    "dev": "docker-compose up"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "5.0.2"
  }
}
